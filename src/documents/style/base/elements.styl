/* 
	font-family padrão serifada pra tirulos
		- nos titulos h_
		- e pra titulos nao semanticos, a classe 'like-h' 

	a stack:
		Georgia: Windows, Mac, iOS (fallback)
		No Android vai pegar a Droid Serif padrão
*/
h1, h2, h3, h4, h5, h6, .like-h {
	font-family: 'Georgia',  serif;
}

/* TITULOS */
h1, h2, h3, h4, h5, h6 {
	color: #222;
	line-height: 1.3;
	font-weight: normal;
}
h1 {
	color: #111;
	font-size: 1.8em;
	margin: 0.6em 0;
}
h2 {
	font-size: 1.3125em;
	margin: 2em 0 1em;
}

hr {
	height: 0;
	border: 0;
	outline: 0;
	border-top: 2px solid #ddd;
}

/* responsive (mobile first) */
@media (min-width: 37em) {
	h1 { font-size: 2.5em; margin: 0 0 0.8em;}
	h2 { font-size: 1.6em; }
}


/* ENFASES */
strong {
	font-weight: 700;
}
em {
	font-style: italic;
	font-weight: 400;
}

/* LISTAS */
ul {
	margin: 1em 0 1em 2em;
}

/* MEDIA */
figure {
	counter-increment: figure;
	clear: both;
	display: block;

	margin: 2.5em 0;
	position: relative;

	img {
		display: inline-block;
		vertical-align: middle;
		max-width: 100%;
	}

	&.video {
		max-width: 600px;
	}
}

figcaption {
	display: block;
	margin-top: 0.5em;
	padding: 0.2em;

	color: #555;
	font-size: 0.875em;
	font-style: italic;	

	&:before {
		content: 'Figura ' counter(figure) ': ';
	}
}


.video-wrapper {
    height: 0;
    padding-top: 0;
    position: relative;
    padding-bottom: 75%; /* default: 4/3 */

	&.r16x9 {
		padding-bottom: 56.25%; /* 16/9 */
	}
	.slideshare & {
		padding-bottom: 83.3723653%; /* especifico do slideshare */
	}
	video, 
	iframe {
	    position: absolute;
	    left: 0;
	    top: 0;
	    width: 100%;
	    height: 100%;
	}
}

/* CITACOES */
blockquote {
	background: #FFF0BB;
	border-left: 10px solid #f90;
	padding: 1em;

	border-top-left-radius: 3px;
	border-bottom-left-radius: 3px;
	box-shadow: 1px 1px 3px rgba(30,30,30,0.25);
}
.post blockquote p {
	margin: 0;
}
